# insurance_premium_prediction
This project is a FastAPI-based REST API developed for an Insurance Premium Prediction System. The API accepts user information such as age, weight, height, annual income, smoking status, city, and occupation, processes the data using a trained Machine Learning model, and returns the predicted insurance premium category along with confidence scores
